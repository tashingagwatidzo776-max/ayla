commit 096264a31b8620202a52e6a64b541389a93c3bbc
Author: Tf Developer <dev@tf.local>
Date:   Tue Sep 22 02:08:00 2026 -0700

    FX brain foundation: MT5-fed terminal data, Terminal sign-in, features/regime/alphas, paper-first FxEngine

diff --git a/src/DongGfx.App/MainWindow.xaml b/src/DongGfx.App/MainWindow.xaml
index d235133..7363d3b 100644
--- a/src/DongGfx.App/MainWindow.xaml
+++ b/src/DongGfx.App/MainWindow.xaml
@@ -911,6 +911,8 @@
                     <Grid.RowDefinitions>
                         <RowDefinition Height="Auto" />
                         <!-- account bar -->
+                        <RowDefinition Height="Auto" />
+                        <!-- sign-in band -->
                         <RowDefinition Height="*" />
                         <!-- main content -->
                     </Grid.RowDefinitions>
@@ -933,6 +935,16 @@
                                 </Border>
                             </StackPanel>
                             <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
+                                <!-- ── DON G FX FX BRAIN ── -->
+                                <Border Style="{StaticResource StatusPill}" Margin="0,0,8,0"
+                                        ToolTip="The forex brain: regime detection + momentum/mean-reversion families on MT5. Paper mode journals signals only; go-live needs the paper soak first.">
+                                    <TextBlock Text="{Binding FxBadge}" FontWeight="Bold" FontSize="11" />
+                                </Border>
+                                <Button Content="FX BRAIN ON/OFF" Command="{Binding ToggleFxBrainCommand}"
+                                        Margin="0,0,6,0" />
+                                <Button Content="GO LIVE" Command="{Binding FxGoLiveCommand}" Margin="0,0,6,0" />
+                                <Button Content="PAPER" Style="{StaticResource GhostButton}"
+                                        Command="{Binding FxGoPaperCommand}" Margin="0,0,12,0" />
                                 <!-- ── THE BRAIN SWITCH ── -->
                                 <Button Content="🧠 BRAIN: ON" Command="{Binding TurnBrainOnCommand}"
                                         Margin="0,0,6,0" />
@@ -944,8 +956,56 @@
                         </DockPanel>
                     </Border>
 
+                    <!-- ── Sign-in band: accounts + PAT/OAuth + bridge, in-tab ── -->
+                    <Border Grid.Row="1" Style="{StaticResource Card}" Padding="10" Margin="0,6,0,0">
+                        <StackPanel>
+                            <DockPanel>
+                                <TextBlock Text="ACCOUNTS" Style="{StaticResource SectionTitle}"
+                                           VerticalAlignment="Center" Margin="0,0,14,0" />
+                                <TextBlock Text="{Binding Mt5BridgeLine}" FontFamily="Consolas" FontSize="11"
+                                           Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" />
+                                <TextBox Text="{Binding TerminalTokenInput, UpdateSourceTrigger=PropertyChanged}"
+                                         FontFamily="Consolas" FontSize="11" MinWidth="240" Margin="18,0,6,0"
+                                         VerticalAlignment="Center"
+                                         ToolTip="Paste a Deriv token (PAT) to sign in — verified via discovery, one row per token" />
+                                <Button Content="Sign in with token" Command="{Binding TerminalImportTokenCommand}"
+                                        Margin="0,0,6,0" VerticalAlignment="Center" />
+                                <Button Content="Sign in with Deriv (OAuth)" Style="{StaticResource GhostButton}"
+                                        Command="{Binding TerminalSignInOAuthCommand}" VerticalAlignment="Center" />
+                            </DockPanel>
+                            <TextBlock Text="{Binding TerminalSignInStatus}" FontSize="11"
+                                       Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"
+                                       TextTrimming="CharacterEllipsis" />
+                            <ItemsControl ItemsSource="{Binding TerminalAccounts}" Margin="0,6,0,0">
+                                <ItemsControl.ItemTemplate>
+                                    <DataTemplate>
+                                        <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="0,4">
+                                            <DockPanel LastChildFill="False">
+                                                <Border Style="{StaticResource StatusPill}" Margin="0,0,8,0">
+                                                    <TextBlock Text="{Binding Kind}" FontWeight="Bold" FontSize="10" />
+                                                </Border>
+                                                <TextBlock Text="{Binding Label}" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0" />
+                                                <TextBlock Text="{Binding StatusText}" FontFamily="Consolas" FontSize="11"
+                                                           Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,10,0" />
+                                                <TextBlock Text="{Binding BalanceText}" FontFamily="Consolas" FontSize="11"
+                                                           Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center" Margin="0,0,10,0" />
+                                                <TextBlock Text="{Binding VerifiedText}" FontFamily="Consolas" FontSize="11"
+                                                           Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" />
+                                                <Button Content="Connect" DockPanel.Dock="Right" Command="{Binding DataContext.TerminalConnectCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
+                                                        CommandParameter="{Binding}" Margin="6,0,0,0" />
+                                                <Button Content="Disconnect" DockPanel.Dock="Right" Style="{StaticResource GhostButton}"
+                                                        Command="{Binding DataContext.TerminalDisconnectAccountCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
+                                                        CommandParameter="{Binding}" />
+                                            </DockPanel>
+                                        </Border>
+                                    </DataTemplate>
+                                </ItemsControl.ItemTemplate>
+                            </ItemsControl>
+                        </StackPanel>
+                    </Border>
+
                     <!-- ── MT5-style workspace ── -->
-                    <DockPanel Grid.Row="1" Margin="0,10,0,0">
+                    <DockPanel Grid.Row="2" Margin="0,10,0,0">
                         <!-- Status bar (bottom) -->
                         <Border DockPanel.Dock="Bottom" Background="{DynamicResource CardBrush}"
                                 Padding="10,6" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
@@ -956,6 +1016,8 @@
                                            Foreground="{StaticResource AccentBrush}" Margin="0,0,16,0" />
                                 <TextBlock Text="{Binding Mt5AccountText}" FontFamily="Consolas" FontSize="11"
                                            Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,16,0" />
+                                <TextBlock Text="{Binding FxStatusText}" FontFamily="Consolas" FontSize="11"
+                                           Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,16,0" />
                                 <TextBlock Text="{Binding DomSourceText}" FontFamily="Consolas" FontSize="11"
                                            Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,16,0" />
                                 <TextBlock Text="{Binding CandleSourceText}" FontFamily="Consolas" FontSize="11"
